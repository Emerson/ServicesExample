/**
*    TODO: Figure out how to get the store registered with the controller. It's
*    not enough to just put it in the needs as store:main.
*/


// import DS from 'ember-data';
// import { test, moduleFor } from 'ember-qunit';

// moduleFor('controller:login-and-registration', "Unit - loginAndRegistrationController", {
//   needs: ['model:user']
// });

// test('it exists', function() {
//   ok(this.subject());
// });

// test('it toggles showLogin', function() {
//   var controller = this.subject();
//   ok(controller.get('showLogin'));
//   controller.send('showRegistration');
//   ok(!controller.get('showLogin'));
//   controller.send('showLogin');
//   ok(controller.get('showLogin'));
// });